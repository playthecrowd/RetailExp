import { GiftingApp } from "@/components/gifting/GiftingApp";

/** Straight into the visitor's private gallery. */
export default function GiftingDemoGallery() {
  return <GiftingApp initial="gallery" />;
}
